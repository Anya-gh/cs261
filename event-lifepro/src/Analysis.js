class Analysis {
    constructor(people, length, selectedInterval) {
      this.currentInterval = 0;
      this.selectedInterval = selectedInterval;
      this.length = length
    }
    calculateMood(){

    }
    checkResponse(responseObj){
      //not made response object yet
    }
  }
  
  module.exports = Analysis;
